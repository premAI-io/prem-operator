apiVersion: v1
kind: Service
metadata:
  name: {{ include "prem-operator-chart.fullname" . }}-controller-manager-metrics-service
  labels:
    app.kubernetes.io/component: kube-rbac-proxy
    app.kubernetes.io/created-by: prem-operator
    app.kubernetes.io/part-of: prem-operator
    control-plane: controller-manager
  {{- include "prem-operator-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  selector:
    control-plane: controller-manager
  {{- include "prem-operator-chart.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.metricsService.ports | toYaml | nindent 2 }}