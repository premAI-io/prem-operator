apiVersion: v1
kind: Service
metadata:
  name: {{ include "saas-controller.fullname" . }}-controller-manager-metrics-service
  labels:
    app.kubernetes.io/component: kube-rbac-proxy
    app.kubernetes.io/created-by: saas-operator
    app.kubernetes.io/part-of: saas-operator
    control-plane: controller-manager
  {{- include "saas-controller.labels" . | nindent 4 }}
spec:
  type: {{ .Values.metricsService.type }}
  selector:
    control-plane: controller-manager
  {{- include "saas-controller.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.metricsService.ports | toYaml | nindent 2 -}}